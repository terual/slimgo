slimgo only works on Linux, as it uses ALSA. If it doesn't compile install the libalsa dev-package.

Use gomake to build slimgo.

TODO:
 - Get information about buffer fullness and size
 - Implement a mechanism to find out the streamtime
 - Remove alsa-go from this repository and use upstream

